name: Tests

on:
  push:
    branches-ignore:
      - main
  pull_request:

jobs:
  test:
    name: Run on Ubuntu
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Cache CRDs
        uses: actions/cache@v4
        with:
          path: .testdata/crds
          key: crds-${{ hashFiles('go.mod', 'Makefile') }}

      - name: Running Tests
        run: |
          go mod tidy
          make test
